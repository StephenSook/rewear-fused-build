/** The app's house easing: an ease-out-expo curve shared by every Motion
 *  transition (reveals, count-ups, the funnel, the router). One source so the
 *  whole UI decelerates with the same hand. */
export const EASE: [number, number, number, number] = [0.16, 1, 0.3, 1];
