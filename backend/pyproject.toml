[project]
name = "rewear-backend"
version = "0.1.0"
description = "REWEAR-FUSED Engines A + B molecular pipelines and artifact server (Pravin)"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
